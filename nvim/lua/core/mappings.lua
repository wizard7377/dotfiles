local map = vim.keymap.set
-- local next_buf = function()
-- 	local cb = vim.api.nvim_buf_get_number()
-- 	vim.api.nvim_set_current_buf(cb)
-- end

-- map('n', '<tab>', next_buf, { desc = 'Next Buffer' })
--map('n', '<s-tab>' , { desc = 'Previous Buffer' })
-- map('n', '<leader>x', function() vim.api.nvim_buf_delete(0, {}) end, { desc = 'Close Buffer' })
